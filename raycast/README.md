# raycast

Raycast の Snippets はシンボリックリンクで自動読み込みされる仕組みがないため、
このディレクトリは `mise run link` の対象外（scripts/link.sh の EXCLUDE）にしてある。
中身は手動インポート用のJSONを置くだけの場所。

## 使い方

Raycast → `Import Snippets` → `raycast/snippets/aios.json` を選択。

## ファイル

- `snippets/aios.json` — AIOS セッション開始プロンプト（keyword: `;me`）
