---
name: walkthrough-review
description: 変更差分を役割別の並列レビューエージェント（責務・命名/コメント・テスト価値・クリティカル/エッジケース/顧客影響・仕様/スコープ）で審査し、指摘をwalkthroughとして出力する。基準は本人の過去レビュー指示から抽出したもの。「レビューして」「差分をチェックして」「この変更を見て」「〜以降をレビュー」といった依頼で使う。指摘はnvimの `,wo` でコード上を辿って確認する。
---

# Walkthrough Review

固定点から `HEAD` までの差分を、役割別のサブエージェント5体で並列レビューし、確定した指摘をwalkthroughで届ける。

## レビュー原則

1. **とにかく削る** — 不要コード・過剰オプション・デッドコード・価値のないテストは削除する。
2. **シンプルさ最優先** — 初見で理解できない複雑さは受け入れない。
3. **コメントよりコード** — コメントで補足が要る時点で、命名か設計を疑う。
4. **既存との一貫性** — 新規コードは周辺の構成・命名・パターンに揃える。
5. **責務の置き場** — 知りすぎているクラス・賢すぎる呼び出し側・神hookを許さない。
6. **設計品質** — データ構造の明瞭さ・依存の向き・テスト容易性・予測可能性・変更容易性。

## 報告ルール

報告しないものは各エージェント定義に従う。共通ルールは1つ: **失敗シナリオが書けない指摘は報告しない**。

## 手順

### 1. 固定点を決めて差分を取る

ユーザー指定がなければ `origin/main` を既定とする。

- `git diff <fixed-point>...HEAD`（3ドット）とコミット一覧を取得する。
- 差分が空なら終了する。

### 2. 軽量パス

条件: typo修正・rename・1〜2行の変更・生成ファイルのみ。

手順: lead が直接差分を読み、レビュー原則で確認してチャットで報告する。walkthrough生成（手順6）は省略する。

### 3. 仕様ソースを特定する

以下の順で探す:

1. コミットメッセージの issue 参照 → `gh issue view` / `gh pr view` で取得する。
2. ユーザーの引数や依頼文。
3. 見つからなければユーザーに確認する。なければ spec-scope-check はスコープ観点のみで実行する。

### 4. スタック別基準を選ぶ

拡張子で判定する（複数該当可）:

- `.rb` → [references/ruby.md](references/ruby.md)
- `.ts` / `.tsx` → [references/frontend.md](references/frontend.md)

### 5. サブエージェント5体を並列起動

`$orchestrate` に従い、1タブ内のpaneに5体を1体ずつ起動する。
プロンプト送信の失敗と起動未達を見逃さないこと。全員の実行開始を確認してから待機に入る。

各エージェントのプロンプトに含める内容:

1. 役割定義の全文 — [agents/](agents/) の対応ファイル
2. スタック別基準の全文 — 手順4で選んだファイル
3. diffコマンドとコミット一覧
4. 仕様全文（あれば。spec-scope-check のみ）
5. 完了報告の定型句（`$orchestrate` に記載）

起動する5体:

| エージェント | 役割 |
|---|---|
| [responsibility-check](agents/responsibility-check.md) | 責務・設計品質。命名の指摘と投機的実装の判定は該当軸に譲る。 |
| [naming-comment-check](agents/naming-comment-check.md) | 命名・コメント（実態・統一・マジックストリング） |
| [test-quality-check](agents/test-quality-check.md) | テスト価値・カバレッジ |
| [critical-impact-check](agents/critical-impact-check.md) | クリティカル・顧客影響 |
| [spec-scope-check](agents/spec-scope-check.md) | 仕様・スコープ（投機的実装の削除判定含む） |

**フォールバック**: herdr が使えない環境では、lead が5役割を1パスで実行する。各指摘に役割タグを付けて手順6に進み、報告冒頭に「フォールバック」と明記する。

### 6. 集約してwalkthroughにする

**検証**: マージ前修正対象は、lead が該当コードと呼び出し元を読み、失敗シナリオが成立するか確認してから載せる。同一指摘が複数軸から出たら1件に統合する。

**チャット報告は結論だけ**（書き方は `$essence`）:

```
結論: マージ前に直すべき 2件 / 削除候補 2件 / 好み 11件
判定: ⚠️ 修正してからマージ（指摘ゼロなら ✅ そのままマージ可）
理由: （1行。マージ可否を左右する判断根拠だけ）
```

指摘ゼロならここで終了（walkthrough生成不要）。

**walkthrough生成**: 指摘の詳細はチャットに書かず、walkthroughのステップにする。

- 1指摘 = 1ステップ。`file:line` は指摘箇所
- 並び順: クリティカル・影響 → 責務・設計 → テスト → 命名・コメント → 仕様・スコープ、軸内は重大度の高い順
- note の書式:

  ```
  [軸][重大度] タイトル

  問題: …
  失敗シナリオ: …（マージ前修正対象は必須）
  直し方: …（before/after コード）
  ```

- JSONの生成・検証・保存の規約は `~/.agents/skills/walkthrough/SKILL.md` を読んで従う。slugは `review-<ブランチ名やPR番号>`
- 保存後、結論3行とあわせて「nvimで `,wo` から辿れます」と案内する
