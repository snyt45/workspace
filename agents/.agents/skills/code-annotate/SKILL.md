---
name: code-annotate
description: 対象コード（PR差分・作業ツリー）の引っかかりやすい構文へ警告レベル付きの注釈を付け、plannotator review の実差分にインラインコメントとして注入する。概要・呼び出し経路・処理の流れもコメントとして配置する。言語を問わず、構文の意味だけでなく「なぜそう書くのか」まで含むコード解説も行う（メンター式。結論→例→動き→なぜ→比較→慣習）。自己完結HTMLは作らない。「注釈を付けて」「annotateして」「code-annotateして」「この構文を解説して」などの依頼で使う。
---

# Annotate

対象コードの「引っかかりやすい構文」へ警告レベル付きの注釈を付け、`plannotator review` の実差分にインラインコメントとして注入する。

読者は経験のあるエンジニア。この構文は何で実際どんな値が入るのかを先回りして潰す。

## 手順

1. **対象を読む** — 差分と実データを特定する（`guides/content.md`）。PR なら `plannotator review <PR_URL>` の diff（`GET /api/diff`）が対象になる。注釈は差分内の行にのみ置ける。差分に無い構文は、それが使われている差分行に置く
2. **注釈を組み立てる** — 概要・呼び出し経路・処理の流れ・構文注釈（L1/L2）を決める（`guides/content.md`）
3. **注入する** — `$plannotator-review` に委譲して起動・注入・回収する
4. **反映する** — フィードバックが返ったら注釈を直して再度注入する
5. **解説が求められたら** — 「この構文を解説して」等、注釈でなく解説の依頼なら注入はせず、`guides/content.md` の「コード解説の組み立て」に従って言語非依存のメンター式で答える（結論→最小例→動き→なぜ→比較→メンタルモデル→実践的慣習。必要なものだけ）

## ファイル

- `guides/content.md` — 注釈の中身の書き方（実データ・警告レベル・配置・essence）
