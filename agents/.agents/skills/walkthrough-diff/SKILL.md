---
name: walkthrough-diff
description: 作業ブランチやPRの差分を上から順に辿るwalkthroughを生成する。「この差分をwalkthroughにして」「PRをwalkthroughで説明して」「変更を順に追いたい」「diffを辿れるようにして」といった依頼で使う。差分の各変更を1ステップずつ、変更理由つきで解説する。AIによる審査はwalkthrough-review、これは変更の理解が目的。
---

# Walkthrough Diff

差分を上から順に辿るwalkthroughを作る。目的はレビュー（審査）ではなく、変更の流れの理解。

## 手順

### 1. 差分を取る

- PR番号/URLが指定されたら: `gh pr diff <PR>` と `gh pr view <PR>`（説明・コミット一覧）
- 指定がなければ: `git diff origin/main...HEAD`（3ドット）とコミット一覧
- 差分が空なら終了する

### 2. ステップを設計する

- 並び順は**差分の上から順**（ファイル→ハンク順）。ただし理解の起点になる変更（エントリポイント・スキーマ・型定義）が後方にある場合は先頭に繰り上げてよい
- 1つの意味のある変更 = 1ステップ。機械的な連鎖（rename の全出現箇所など）は代表1箇所にまとめ、note に「他N箇所も同様」と書く
- `file:line` は**HEAD側の行番号**。削除のみの変更は、その削除を参照している周辺の残存行に置く
- note に書くこと: 何が変わったか（before→after）、なぜ変わったか（コミットメッセージ・PR説明から）、他ステップとの繋がり
- 新規追加ファイルは冒頭に1ステップ置き、役割を説明してから内部の要点に進む

### 3. 生成・検証・保存

JSONの生成・検証・保存の規約は `~/.agents/skills/walkthrough/SKILL.md` を読んで従う。

- slug: `diff-<ブランチ名やPR番号>`
- `commit` は HEAD の SHA（PRの場合はチェックアウトしているコミット）
- 保存後、ステップ数と概要1行を報告し「nvimで `,wo` から辿れます」と案内する
