-- ==========================================================================
-- キーマップピッカー: <leader>? で登録済みキーマップを選択・実行
-- ==========================================================================

vim.keymap.set("n", "<leader>?", function()
	require("features.keymap_picker").open()
end, { desc = "[Palette] キーマップピッカー" })