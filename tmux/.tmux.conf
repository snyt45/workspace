set-option -g status-bg default
set-option -g status-style bg=default

set-option -g window-style 'bg=default'
set-option -g window-active-style 'bg=default'
set-option -g pane-border-style 'fg=#585b70'
set-option -g pane-active-border-style 'fg=#89b4fa'

# prefixキーをC-tに変更する
set-option -g prefix C-t

# フォーカスイベントを有効にする（外部変更の自動検知に必要）
set-option -g focus-events on

#マウス操作を有効にする
set-option -g mouse on

# History
set-option -g history-limit 100000

# スクロールアップするとコピーモードに入る
bind-key -n WheelUpPane if-shell -F -t = "#{mouse_any_flag}" "send-keys -M" "if -Ft= '#{pane_in_mode}' 'send-keys -M' 'select-pane -t=; copy-mode -e; send-keys -M'"

# 最後までスクロールダウンするとコピーモードを抜ける
bind-key -n WheelDownPane select-pane -t= \; send-keys -M

# ペイン移動（Prefix付き）
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R

# ペインを垂直分割する
bind v split-window -h -c '#{pane_current_path}'
# ペインを水平分割する
bind s split-window -v -c '#{pane_current_path}'

# ペイン番号を表示
bind i display-panes

# ペインをズーム（全画面）
bind t resize-pane -Z
# ペインを縦方向のみズーム（横レイアウト維持）
bind T run-shell "$HOME/bin/tmux-partial-zoom"

# ペインを終了
bind q kill-pane

# ウィンドウ内の複数ペイン同時操作 ON
bind e setw synchronize-panes on
# ウィンドウ内の複数ペイン同時操作 OFF
bind E setw synchronize-panes off

# セッション新規作成 (名前をプロンプトで入力)
bind N command-prompt -p "New session:" "new-session -s '%%'"
# セッション一覧から選択 (choose-tree)
bind S choose-tree -Zs
# 次/前のセッションに切り替え (デフォルトのウィンドウ移動を上書き)
bind -r n switch-client -n
bind -r p switch-client -p

# Plugins (tpm)
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'catppuccin/tmux#v2.3.0'

# Catppuccin
set -g @catppuccin_flavor 'mocha'
set -g @catppuccin_window_status_style 'rounded'
set -g @catppuccin_window_default_text ' #W'
set -g @catppuccin_window_current_text ' #W'

# Initialize TMUX plugin manager (最下部固定)
run '~/.tmux/plugins/tpm/tpm'

# ステータスライン (tpm読み込み後に設定する必要あり)
set -g status-left "#{E:@catppuccin_status_session}"
set -g status-right "#[fg=#{@thm_mauve}]#{?pane_synchronized,sync on,sync off} "
